using DALPEGASE;
using System;
using System.Collections.Generic;
using System.IO;
using System.Linq;
using System.Web;

namespace GenerateurDFUSafir.Models
{
    public class GestionOFX3
    {
    //    public static bool SoldeOFX3(OF_PROD_TRAITE of )
    //    {
    //        DateTime now = DateTime.Now;
    //        string path = 
    //       //FileStream fileStream = new FileStream(string.Concat(path, "\\Soldeof", of.NMROF.ToUpper(), (1).ToString("00"), ".cmd"), FileMode.Create);
    ////        StreamWriter writer = new StreamWriter(fileStream);

    ////        writer.WriteLine(string.Concat(StructLabel.LABELNAME, "=\"", Resource1.LABELNAME_PNC, "\""));
    ////        //writer.WriteLine(string.Concat(StructLabel.LABELNAME, "=\"", "57X21 DATAMATRIX.Lab", "\""));
    ////        writer.WriteLine(string.Concat(StructLabel.CODE_PRODUIT1, "=\"", op.Item, "\""));
    ////        writer.WriteLine(string.Concat(StructLabel.NC, "=\"", op.NmrChronoS, "\""));
    ////        writer.WriteLine(string.Concat(StructLabel.AUTRE, "=\"", "Qtr:", ((int)op.Qtr).ToString("00"), "\""));

    ////        writer.WriteLine(string.Concat(StructLabel.QUANTITY, "=\"", "1", "\""));
    ////        writer.Close();
    ////        fileStream.Close();

    ////        return true;
    ////        // creer un repertoire de op si pas déjà creer
    ////        // sauvegarder les fichier de script X3
    ////        // copie ces fichier dans job X3

    ////        // creer repertoire
             
    ////        // creer fichier txt

    ////        //creer job

    //    }
    }
}